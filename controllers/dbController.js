'use strict'

var conf = require('../conf');
var mongoose = require('mongoose');
//
// var db = mongoose.connect('mongodb://' + conf.get('database:url') + '/' +
//             conf.get('database:name') + ':' + conf.get('database:port'));
// var db = mongoose.connect('mongodb://localhost/test');
